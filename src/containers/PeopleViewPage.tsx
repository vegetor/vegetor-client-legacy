import React from 'react'
import { Header } from '../components'

const PeopleViewPage: React.FC = () => (
    <section>
        <Header authorized={false} />
    </section>
)

export default PeopleViewPage
